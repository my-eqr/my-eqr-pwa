export enum MODES {
    RESCUE = 'Rescue',
    REPAIR = 'Repair',
    TRAINING = 'Training',
}

export enum COOKIES {
    ACTIVE_MODE = 'activeMode',
}

export enum ORIENTATION {
    PORTRAIT = 'portrait-primary',
    LANDSCAPE = 'landscape-primary',
}
